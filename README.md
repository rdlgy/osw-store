# Outside World (OSW) — Store

## Run locally (optional)
npm install
npm run dev

## Deploy
See the deployment steps shared in chat: push this folder to GitHub, import into Vercel, connect your domain.
